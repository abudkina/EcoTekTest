services:
  - type: web
    name: ecotek-test
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
